ACCEPT VNO PROMPT '부서번호 입력:'

DECLARE
	VDEPTNO DEPT.DEPTNO%TYPE:= &VNO;
	VDNAME DEPT.DNAME%TYPE ;
	VLOC DEPT.LOC%TYPE;


BEGIN
	SELECT *
		INTO VDEPTNO, VDNAME, VLOC
	FROM DEPT
	WHERE DETPNO = VDEPTNO;
	DBMS_OUTPUT.PUT_LINE(VDEPTNO||'		'||VDNAME||'		'||VLOC);
END;
/