-- PACKAGE
-- 1. SPEC: 어떤 함수와 프로시저로 구성되어 있는지 목록
-- 2. BODY: 실질적인 소스 코드

CREATE OR REPLACE PACKAGE MY_PACK

IS
	--	PACKAGE SPEC
	FUNCTION ANNSAL (VEMPNO EMP.EMPNO%TYPE)
	RETURN NUMBER

	PROCEDURE RAISE_SAL (VEMPNO EMP.EMPNO%TYPE, VRATIO NUMBER)


END;
/